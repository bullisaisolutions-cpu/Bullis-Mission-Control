import{load,save,reset,backup}from"./store.js";
let state=load(),view="dashboard",recognition=null,voiceListening=false;
const app=document.querySelector("#app"),title=document.querySelector("#title");
const blessing="May you walk in the supernatural favor, blessing, abundance, and ability of the Most High God! May you open your eyes and see your dreams, desires, and destiny manifest in a supernaturally accelerated fashion, in Jesus' name and sealed by the Spirit of the Most High God, AMEN!";
const esc=s=>String(s??"").replace(/[&<>\"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
const names={dashboard:"Dashboard",missions:"Missions",rachel:"Rachel",calendar:"Calendar",prayer:"Prayer",notes:"Notes",people:"People",settings:"Settings"};
function speak(text){if(!("speechSynthesis" in window)){alert("Speech playback is not supported in this browser.");return}speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.rate=.92;u.pitch=1;u.volume=1;const voices=speechSynthesis.getVoices();const preferred=voices.find(v=>/female|zira|samantha|google us english/i.test(v.name))||voices.find(v=>v.lang?.startsWith("en"))||voices[0];if(preferred)u.voice=preferred;speechSynthesis.speak(u)}
function startGreetingListener(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){alert("Voice greeting is not supported in this browser.");return}if(recognition){try{recognition.stop()}catch{}}recognition=new SR();recognition.lang="en-US";recognition.continuous=true;recognition.interimResults=false;recognition.onstart=()=>{voiceListening=true;render()};recognition.onresult=e=>{const heard=Array.from(e.results).slice(e.resultIndex).map(r=>r[0].transcript).join(" ").toLowerCase();if(heard.includes("good morning rachel")){speak(blessing);state.messages.push({id:crypto.randomUUID(),role:"rachel",text:blessing});save(state)}};recognition.onerror=e=>{if(e.error!=="no-speech")console.warn("Voice greeting error:",e.error)};recognition.onend=()=>{if(voiceListening){try{recognition.start()}catch{}}};recognition.start()}
function stopGreetingListener(){voiceListening=false;if(recognition){try{recognition.stop()}catch{}}render()}
function dashboard(){const done=state.missions.filter(m=>m.done).length,total=state.missions.length,pct=total?Math.round(done/total*100):0,next=state.missions.find(m=>m.active&&!m.done)||state.missions.find(m=>!m.done);return `<div class="card hero"><p class="muted">RACHEL RECITES GOD'S BLESSING TO HIS ANOINTED</p><h2>Centralize the day with God</h2><p>${esc(blessing)}</p><div class="actions"><button class="btn" data-act="speakBlessing">🔊 Hear the Blessing</button><button class="btn" data-act="${voiceListening?"stopVoice":"startVoice"}">${voiceListening?"🛑 Stop Voice Greeting":"🎙 Enable “Good morning, Rachel”"}</button></div><p class="muted">${voiceListening?"Listening now. Say “Good morning, Rachel.”":"After microphone permission is enabled, saying “Good morning, Rachel” while the app is open will make this the first thing Rachel says."}</p></div><div class="grid cols2" style="margin-top:1rem"><div class="card hero"><p class="muted">MISSION ADVISOR</p><h2>Rachel</h2><p>${next?`Your next move is <b>${esc(next.title)}</b>. Keep it small and finishable.`:"Everything listed is complete."}</p><button class="btn" data-act="startNext">Start Next Mission</button></div><div class="card"><h2>Current Mission</h2>${next?`<div class="name">${esc(next.title)}</div><p class="muted">${esc(next.details)}</p>`:"<p class='muted'>No open mission.</p>"}</div></div><div class="grid cols4" style="margin-top:1rem"><div class="card stat"><strong>${done}</strong><span>Complete</span></div><div class="card stat"><strong>${total-done}</strong><span>Remaining</span></div><div class="card stat"><strong>${pct}%</strong><span>Progress</span></div><div class="card stat"><strong>${state.prayers.length}</strong><span>Prayers</span></div></div>`}
function listPage(key,label,icon){const rows=state[key].map(x=>`<div class="item"><span>${icon}</span><div><div class="name">${esc(x.title)}</div><div class="muted">${esc(x.details)}</div></div><button class="btn small" data-del="${key}" data-id="${x.id}">Delete</button></div>`).join("");return `<div class="card"><div class="row" style="justify-content:space-between"><h2>${label}</h2><button class="btn" data-new="${key}">Add</button></div><div class="list">${rows||"<p class='muted'>Nothing added yet.</p>"}</div></div>`}
function missions(){const rows=state.missions.map(m=>`<div class="item ${m.done?"done":""}"><input type="checkbox" data-check="${m.id}" ${m.done?"checked":""}><div><div class="name">${esc(m.title)}</div><div class="muted">${esc(m.details)} ${m.active?"• Active":""}</div></div><div class="actions"><button class="btn small" data-start="${m.id}">Start</button><button class="btn small" data-delete-mission="${m.id}">Delete</button></div></div>`).join("");return `<div class="card"><div class="row" style="justify-content:space-between"><h2>Mission Center</h2><button class="btn" data-new="missions">Add Mission</button></div><div class="list">${rows}</div></div>`}
function rachel(){const msgs=state.messages.map(m=>`<div class="card" style="margin:.5rem 0;max-width:80%;${m.role==="user"?"margin-left:auto;background:#2f5f9d;color:white":""}">${esc(m.text)}</div>`).join("");return `<div class="grid cols2"><div class="card"><h2>Rachel</h2><div>${msgs}</div><textarea id="rachelInput" placeholder="Tell Rachel what you need..."></textarea><button class="btn" id="sendRachel" style="margin-top:.5rem">Send</button></div><div class="card hero"><h2>God-Centered Mission Advisor</h2><p>Rachel begins the day by reciting God's blessing to His anointed, then helps turn faith, purpose, and responsibility into the next faithful action.</p><button class="btn" data-act="speakBlessing">Hear the Blessing</button></div></div>`}
function settings(){return `<div class="grid cols2"><div class="card"><h2>Backup</h2><button class="btn" data-act="backup">Export Backup</button><p><label class="btn">Restore Backup<input type="file" id="restore" accept="application/json" hidden></label></p></div><div class="card"><h2>System</h2><p>Version: 2.1.0-develop</p><button class="btn" data-act="reset">Reset Local Data</button></div><div class="card hero"><p class="muted">ABOUT</p><h2>Bullis Mission Control</h2><p><strong>Version 2.1.0-develop</strong></p><p>Built by <strong>Bullis AI Solutions</strong>.</p><p>BMC is unapologetically God-centered and presents a daily nudge toward Christ, discipleship, faithful action, and the Great Commission of Matthew 28:18–20.</p></div><div class="card"><h2>Voice Greeting</h2><p>Enable microphone access, then say <strong>“Good morning, Rachel.”</strong> Rachel will recite God's blessing to His anointed.</p><button class="btn" data-act="${voiceListening?"stopVoice":"startVoice"}">${voiceListening?"Stop Listening":"Enable Voice Greeting"}</button></div></div>`}
function render(){title.textContent=names[view];document.querySelectorAll("nav button").forEach(b=>b.classList.toggle("active",b.dataset.view===view));app.innerHTML=view==="dashboard"?dashboard():view==="missions"?missions():view==="rachel"?rachel():view==="calendar"?listPage("events","Calendar","📅"):view==="prayer"?listPage("prayers","Prayer Journal","🙏"):view==="notes"?listPage("notes","Notes","📝"):view==="people"?listPage("people","People","👤"):settings();bind()}
function persist(){save(state);render()}
function add(key){const t=prompt("Title:");if(!t)return;const d=prompt("Details:")||"";state[key].push({id:crypto.randomUUID(),title:t,details:d,...(key==="missions"?{done:false,active:false}:{})});persist()}
function bind(){document.querySelectorAll("[data-new]").forEach(b=>b.onclick=()=>add(b.dataset.new));document.querySelectorAll("[data-del]").forEach(b=>b.onclick=()=>{state[b.dataset.del]=state[b.dataset.del].filter(x=>x.id!==b.dataset.id);persist()});document.querySelectorAll("[data-check]").forEach(c=>c.onchange=()=>{const m=state.missions.find(x=>x.id===c.dataset.check);m.done=c.checked;m.active=false;persist()});document.querySelectorAll("[data-start]").forEach(b=>b.onclick=()=>{state.missions.forEach(m=>m.active=false);state.missions.find(x=>x.id===b.dataset.start).active=true;persist()});document.querySelectorAll("[data-delete-mission]").forEach(b=>b.onclick=()=>{state.missions=state.missions.filter(x=>x.id!==b.dataset.deleteMission);persist()});document.querySelectorAll("[data-act]").forEach(b=>b.onclick=()=>{if(b.dataset.act==="startNext"){state.missions.forEach(m=>m.active=false);const n=state.missions.find(m=>!m.done);if(n)n.active=true;persist()}if(b.dataset.act==="backup")backup(state);if(b.dataset.act==="reset"&&confirm("Reset BMC local data?")){state=reset();render()}if(b.dataset.act==="speakBlessing")speak(blessing);if(b.dataset.act==="startVoice")startGreetingListener();if(b.dataset.act==="stopVoice")stopGreetingListener()});const send=document.querySelector("#sendRachel");if(send)send.onclick=()=>{const i=document.querySelector("#rachelInput"),txt=i.value.trim();if(!txt)return;state.messages.push({id:crypto.randomUUID(),role:"user",text:txt},{id:crypto.randomUUID(),role:"rachel",text:"Turn that into one visible next action. Add it as a mission, then start before adding anything else."});persist()};const restore=document.querySelector("#restore");if(restore)restore.onchange=async e=>{try{state=JSON.parse(await e.target.files[0].text());save(state);render()}catch{alert("Invalid backup file.")}}}
document.querySelector("nav").onclick=e=>{const b=e.target.closest("[data-view]");if(b){view=b.dataset.view;render()}};
const q=document.querySelector("#quickDialog");document.querySelector("#quickAdd").onclick=()=>q.showModal();document.querySelector("#saveItem").onclick=e=>{const key=document.querySelector("#type").value,t=document.querySelector("#itemTitle").value.trim(),d=document.querySelector("#details").value.trim();if(!t){e.preventDefault();return}state[key].push({id:crypto.randomUUID(),title:t,details:d,...(key==="missions"?{done:false,active:false}:{})});save(state);document.querySelector("#quickForm").reset();render()};
if("serviceWorker"in navigator)addEventListener("load",()=>navigator.serviceWorker.register("service-worker.js"));render();
